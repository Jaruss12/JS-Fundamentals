//* literals

//* js defiines 2 types of values: fixed andd variable values
//* fixed are called literals variable values are called variables

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());
console.log(days.length);

let tired = true;

let soup ={
    a:'chicken noodle soup',
    b: 'tomato',
    c:'beef and Barley',
};

console.log(soup.c);